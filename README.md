# Scanovich Content Bot

Telegram-бот "Карманная фотостудия для маркетплейсов" — генерация профессиональных изображений товаров с инфографикой для маркетплейсов с использованием ASR, LLM и Google Gemini API.

**Бот:** [@scanovich_imagecontent_bot](https://t.me/scanovich_imagecontent_bot)  
**Владелец:** @WuWeiBuild

## Особенности

- 🤖 **Умное извлечение характеристик:** Автоматически анализирует описание товара и определяет тип (одежда, электроника, косметика, транспорт и т.д.)
- 📊 **Инфографика по best practices:** Создает инфографику с учетом визуальной иерархии, правила 3-5, читаемости для маркетплейсов
- 🎯 **Сохранение оригинального товара:** Товар на фото остается идентичным оригиналу, меняется только фон, освещение и добавляется инфографика
- 🎤 **Голосовой ввод:** Поддержка голосовых сообщений с автоматической транскрибацией через ASR
- 🔄 **Безлимит для разработчика:** Владелец бота имеет безлимитное использование

## Быстрый запуск

```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content && source .venv/bin/activate && PYTHONPATH=. python -m src.bot.main
```

Или через скрипт:
```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content && ./start_bot.sh
```

## Команды бота

- `/start` — приветственное сообщение и инструкции
- `/gen` или "ген" — начать процесс генерации изображения
- `/stats` — статистика использования бота (только для владельца)
- `/user <user_id>` — детальная статистика конкретного пользователя (только для владельца)

## Описание

Бот позволяет продавцам маркетплейсов создавать профессиональные изображения товаров:
- Отправка 1-2 фото товара
- Описание задачи текстом или голосом
- Получение готовых изображений для карточек товара

## Архитектура

- **ASR сервис** (порт 8001): Распознавание речи из голосовых сообщений (русский язык)
- **VLLM сервис** (порт 8002): Нормализация брифа пользователя в структурированный промпт
  - Возвращает `normalized_brief` на русском языке для пользователя
  - Возвращает `prompt_for_model` на английском языке для Gemini API
- **Google Gemini API**: Генерация изображений товаров (использует английский промпт)
- **Redis**: Хранение состояния FSM диалогов

## Требования

- Python 3.12+
- uv (менеджер пакетов)
- Доступ к серверу с ASR и VLLM сервисами
- API ключ Google Gemini

## Установка

1. Клонировать репозиторий:
```bash
git clone git@github.com:FUYOH666/scanovich-infographic-bot.git
cd scanovich-infographic-bot
```

**Примечание:** Репозиторий является приватным. Для доступа обратитесь к владельцу @WuWeiBuild.

**GitHub репозиторий:** [scanovich-infographic-bot](https://github.com/FUYOH666/scanovich-infographic-bot) (приватный)

2. Установить зависимости:
```bash
uv sync
```

3. Настроить переменные окружения:
```bash
cp .env.example .env
# Отредактировать .env и указать все необходимые параметры
```

4. Настроить `config.yaml` (опционально, значения можно переопределить через .env)

## Конфигурация

### Переменные окружения (.env)

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_OWNER_ID=8347160745
TELEGRAM_OWNER_USERNAME=WuWeiBuild

# ASR Service
ASR_HOST=100.93.82.48
ASR_PORT=8001
ASR_TIMEOUT=60

# VLLM Service
VLLM_HOST=100.93.82.48
VLLM_PORT=8002
VLLM_MODEL=models/Qwen3-30B-A3B-Instruct-2507-AWQ-4bit

# Google Gemini
GEMINI_API_KEY=your_gemini_api_key

# Redis
REDIS_HOST=100.93.82.48
REDIS_PORT=6380
REDIS_DB=0

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=text
```

## Запуск

### Вариант 1: Docker (рекомендуется для продакшена)

**Требования:**
- Docker и Docker Compose установлены на сервере
- Файл `.env` настроен с правильными значениями

**Сборка и запуск:**
```bash
# Сборка образа
docker build -t scanovich-content-bot .

# Или через docker-compose (рекомендуется)
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down
```

**Проверка работы:**
```bash
# Статус контейнера
docker-compose ps

# Логи в реальном времени
docker-compose logs -f bot

# Вход в контейнер (для отладки)
docker-compose exec bot sh
```

**Обновление:**
```bash
# Остановить старый контейнер
docker-compose down

# Обновить код (git pull)

# Пересобрать и запустить
docker-compose up -d --build
```

**Развертывание на сервере:**

1. **Подготовка сервера:**
   ```bash
   # Установка Docker и Docker Compose (если не установлены)
   # Ubuntu/Debian:
   sudo apt-get update
   sudo apt-get install -y docker.io docker-compose
   sudo systemctl enable docker
   sudo systemctl start docker
   
   # Добавить пользователя в группу docker (опционально)
   sudo usermod -aG docker $USER
   ```

2. **Клонирование и настройка:**
   ```bash
   # Клонировать репозиторий
   git clone <repository-url>
   cd Scanovich-Content
   
   # Создать .env файл из примера
   cp .env.example .env
   # Отредактировать .env с правильными значениями
   nano .env
   
   # Убедиться, что config.yaml настроен правильно
   cat config.yaml
   ```

3. **Проверка доступности внешних сервисов:**
   ```bash
   # Проверить доступность ASR (порт 8001)
   curl http://100.93.82.48:8001/health
   
   # Проверить доступность VLLM (порт 8002)
   curl http://100.93.82.48:8002/health
   
   # Проверить доступность Redis (порт 6380)
   redis-cli -h 100.93.82.48 -p 6380 ping
   ```

4. **Сборка и запуск:**
   ```bash
   # Сборка образа
   docker-compose build
   
   # Запуск в фоновом режиме
   docker-compose up -d
   
   # Проверка статуса
   docker-compose ps
   
   # Просмотр логов
   docker-compose logs -f bot
   ```

5. **Мониторинг и обслуживание:**
   ```bash
   # Просмотр логов в реальном времени
   docker-compose logs -f bot
   
   # Перезапуск бота
   docker-compose restart bot
   
   # Остановка
   docker-compose down
   
   # Остановка с удалением volumes (осторожно!)
   docker-compose down -v
   ```

**Важно для продакшена:**
- Убедитесь, что `.env` файл не попадает в git (он уже в `.gitignore`)
- Настройте автоматический перезапуск контейнера при перезагрузке сервера (`restart: unless-stopped` уже настроен)
- Настройте ротацию логов, если используете монтирование `./logs`
- Проверьте доступность внешних сервисов (ASR, VLLM, Redis) с сервера

### Вариант 2: Через скрипт (локальная разработка)
```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content
./start_bot.sh
```

### Вариант 3: Напрямую через Python
```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content
source .venv/bin/activate
PYTHONPATH=. python -m src.bot.main
```

### Вариант 4: Одна команда из любого места
```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content && source .venv/bin/activate && PYTHONPATH=. python -m src.bot.main
```

### Важно: только один экземпляр бота

Telegram не позволяет запускать несколько экземпляров бота одновременно. Если видите ошибку `TelegramConflictError`, остановите все процессы:

**Для локального запуска:**
```bash
pkill -f "src.bot.main"
```

**Для Docker:**
```bash
docker-compose down
# Затем запустите заново
docker-compose up -d
```

## Система лимитов и аналитика

### Бесплатный тариф

- Каждый новый пользователь получает **10 бесплатных запросов**
- Лимит не сбрасывается (10 запросов навсегда)
- После исчерпания лимита пользователь получает сообщение с контактом владельца
- Команды `/start` и `/stats` не считаются запросами

### Аналитика для владельца

Владелец бота может использовать команды для просмотра статистики:

**`/stats`** — общая статистика бота:
- Общее количество пользователей
- Общее количество запросов
- Количество активных пользователей сегодня
- Топ-10 пользователей по количеству запросов (с датами первого и последнего визита)

**`/user <user_id>`** — детальная статистика конкретного пользователя:
- ID пользователя
- Username
- Количество запросов
- Дата первого визита
- Дата последнего визита

**Пример:** `/user 123456789`

### Хранение данных

Все данные хранятся в Redis:
- Счетчики запросов пользователей
- Метаданные пользователей (дата регистрации, последняя активность)
- Общая статистика

## Использование

### Основной поток работы

1. **Отправить `/start`** — получить приветственное сообщение с инструкциями

2. **Отправить `/gen` или "ген"** — начать процесс генерации
   - Бот переходит в состояние ожидания фотографий

3. **Отправить 1 фото товара**
   - После получения фото бот отвечает: "✅ Фото получено!"
   - Автоматически переходит в ожидание брифа

4. **Описать задачу текстом или голосовым сообщением**
   - Если голосовое: бот транскрибирует через ASR и показывает распознанный текст
   - Затем умная система анализирует описание через LLM (VLLM):
     * Определяет тип товара (одежда, электроника, косметика и т.д.)
     * Извлекает ключевые характеристики в зависимости от типа товара
     * Выделяет 3-5 главных преимуществ
     * Приоритизирует информацию для инфографики
   - Генерирует изображение через Gemini с учетом извлеченных характеристик и best practices
   - **Важно:** Товар на фото остается идентичным оригиналу, меняется только фон, освещение и добавляется инфографика

5. **Получить готовые изображения** (1 вариант с инфографикой)

### Команды после генерации

- **`/gen`** — начать новую генерацию (сбросит текущее состояние)

### Примеры запросов

**Текстовые (на русском языке):**
- "Футболка из хлопка, размеры S-XL, цвета: белый, черный, синий. Преимущества: дышащая ткань, не садится после стирки, удобный крой. Для Wildberries."
- "Для Wildberries, товар - стеклянная бутылка 500мл, прозрачная. Преимущества: экологичность, герметичность, стильный дизайн. Характеристики: высота 20см, диаметр 7см, вес 300г."
- "Создай картинку с преимуществами товара: прочность, долговечность, легкий уход."
- "Только фото без инфографики" (если не нужна инфографика)

**Голосовые (на русском языке):**
- Можно описать задачу голосом — бот автоматически распознает речь через ASR
- ASR поддерживает русский язык

## Структура проекта

```
Scanovich-Content/
├── src/
│   ├── bot/              # Основной код бота
│   │   ├── handlers/     # Обработчики команд
│   │   │   ├── start.py  # /start команда
│   │   │   ├── gen.py    # /gen команда и генерация
│   │   │   └── photos.py # Обработка фотографий
│   │   ├── middleware.py # Middleware для логирования и ошибок
│   │   ├── states.py     # FSM состояния
│   │   ├── storage.py    # Redis storage
│   │   └── main.py       # Точка входа
│   ├── services/         # Клиенты внешних сервисов
│   │   ├── asr_client.py # ASR транскрибация (порт 8001)
│   │   ├── llm_client.py # LLM нормализация (VLLM, порт 8002)
│   │   └── gemini_client.py # Gemini генерация изображений
│   ├── utils/            # Утилиты
│   │   ├── file_handler.py # Работа с файлами (скачивание фото/аудио)
│   │   └── prompt_builder.py # Построение промптов
│   └── config.py         # Конфигурация (pydantic-settings)
├── config.yaml           # Конфигурация сервисов
├── .env                  # Переменные окружения (не в git)
├── .env.example          # Пример переменных окружения
├── Dockerfile            # Docker образ для контейнеризации
├── docker-compose.yml    # Docker Compose конфигурация
├── .dockerignore         # Исключения для Docker сборки
├── start_bot.sh          # Скрипт запуска бота (локально)
├── pyproject.toml        # Зависимости проекта
└── README.md             # Документация
```

## Архитектура обработки запросов

### Поток обработки генерации изображения

```
Пользователь → Бот
    ↓
1. /gen → WAITING_PHOTOS
    ↓
2. Фото → Скачивание → Сохранение в Redis (как string)
    ↓
3. Текст/Голос → WAITING_BRIEF
    ↓
4. Если голос:
   → Скачивание → ASR (порт 8001) → Текст на русском
    ↓
5. Текст (русский) → LLM умная нормализация (VLLM, порт 8002)
   → Анализ типа товара и извлечение характеристик
   → Приоритизация информации для инфографики
   → normalized_brief: описание на русском для пользователя
   → prompt_for_model: детальный промпт на английском для Gemini с извлеченными характеристиками
    ↓
6. prompt_for_model + Фото → Gemini API
   → Генерация изображений с инфографикой
   → Сохранение оригинального товара без изменений
    ↓
7. Результат → Пользователь
   → SHOW_RESULT (ожидание /gen для новой генерации)
```

### Состояния FSM

- **IDLE** — ожидание команд (/start)
- **WAITING_PHOTOS** — ожидание 1 фото товара
- **WAITING_BRIEF** — ожидание текстового или голосового ТЗ
- **PROCESSING** — обработка (ASR → LLM → Gemini)
- **SHOW_RESULT** — показ результата, ожидание действий

## Технические детали

### Умная система извлечения характеристик

Система автоматически анализирует описание товара и извлекает ключевые характеристики:

- **Определение типа товара:** Одежда, электроника, косметика, дом/интерьер, продукты
- **Извлечение характеристик по типу:**
  - Одежда: размеры, материал, цвет, особенности ткани, уход
  - Электроника: мощность, размеры, функции, совместимость
  - Косметика: объем, состав, тип кожи, эффект
  - Дом/Интерьер: размеры, материал, стиль
  - Продукты: вес/объем, состав, особенности
- **Приоритизация:** Выделяет 3-5 самых важных характеристик для покупателя
- **Извлечение преимуществ:** Формулирует преимущества из характеристик или описания

### Best Practices для маркетплейсов

Инфографика создается с учетом best practices:

- **Визуальная иерархия:** Важные характеристики крупнее (18-24pt), преимущества средним размером (14-16pt)
- **Правило 3-5:** Максимум 3-5 ключевых преимуществ
- **Читаемость:** Высокий контраст (минимум 4.5:1), крупные шрифты
- **Сканируемость:** Информация читается за 3-5 секунд
- **F-паттерн:** Важная информация слева и сверху
- **Эмоциональные триггеры:** Акцент на выгодах покупателя, а не просто характеристиках

### Сохранение оригинального товара

**Критически важно:** При работе с входным изображением товар остается идентичным оригиналу:

- ✅ **Можно менять:** Фон, освещение, инфографику вокруг товара
- ❌ **Нельзя менять:** Форму, цвет, детали, геометрию товара

Система явно указывает в промптах для Gemini необходимость сохранения оригинального товара без изменений.

### Сериализация данных в Redis

- **Path объекты** конвертируются в строки при сохранении в Redis
- При чтении строки автоматически конвертируются обратно в Path
- Это необходимо, так как Path объекты не сериализуются в JSON

### Обработка ошибок

- Все ошибки логируются с полным traceback
- Состояние сохраняется даже при ошибках (если возможно)
- Временные файлы удаляются в `finally` блоках
- Пользователь получает понятные сообщения об ошибках
- Владелец бота получает уведомления о критических ошибках

### Таймауты

- **ASR**: 60 секунд (настраивается через `ASR_TIMEOUT`)
- **VLLM**: 30 секунд (настраивается через `VLLM_TIMEOUT`)
- **Gemini**: 120 секунд (настраивается через `GEMINI_TIMEOUT`)

## Управление статистикой и пользователями

### Обнуление счетчиков запросов

Для обнуления счетчиков запросов всех пользователей используйте скрипт:

```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content
source .venv/bin/activate
PYTHONPATH=. python scripts/reset_user_requests.py
```

Скрипт:
- Найдет всех пользователей с запросами
- Обнулит их счетчики запросов
- Предложит обнулить общий счетчик запросов

### Просмотр статистики через Redis CLI

Вы можете напрямую подключиться к Redis и посмотреть данные:

```bash
redis-cli -h 100.93.82.48 -p 6380
```

**Полезные команды:**

```redis
# Посмотреть всех пользователей с запросами
KEYS user_requests:*

# Посмотреть количество запросов конкретного пользователя
GET user_requests:123456789

# Посмотреть метаданные пользователя
HGETALL user_meta:123456789

# Общая статистика
GET stats:total_users
GET stats:total_requests
GET stats:active_today:2025-01-18  # замените на текущую дату

# Посмотреть всех уникальных пользователей
SMEMBERS stats:unique_users
```

**Структура данных:**

- `user_requests:{user_id}` — количество запросов пользователя (число)
- `user_meta:{user_id}` — метаданные пользователя (hash):
  - `first_seen` — дата первого визита (ISO формат)
  - `last_seen` — дата последнего визита (ISO формат)
  - `username` — Telegram username
  - `total_requests` — общее количество запросов
- `stats:total_users` — общее количество пользователей
- `stats:total_requests` — общее количество запросов
- `stats:active_today:{date}` — количество активных пользователей за день
- `stats:unique_users` — множество всех уникальных user_id

## Разработка

### Установка зависимостей для разработки

```bash
uv sync --dev
```

### Линтинг

```bash
uv run ruff check src/
uv run ruff format src/
```

### Типизация

```bash
uv run pyright src/
```

### Тестирование локально

1. Убедитесь, что все сервисы доступны:
   - ASR: `curl http://100.93.82.48:8001/health`
   - VLLM: `curl http://100.93.82.48:8002/v1/models`
   - Redis: `redis-cli -h 100.93.82.48 -p 6380 ping`

2. Запустите бота и проверьте логи на наличие ошибок

3. Протестируйте полный цикл: фото → голос → генерация

## Логирование

Бот использует детальное структурированное логирование для диагностики:

### Формат логов
- Формат: `время · уровень · сервис · сообщение`
- Детальные логи каждого действия пользователя с форматом: `[USER {id}] [STATE: {state}] {action}`

### Что логируется

1. **Действия пользователя:**
   - Команды (/start, /gen)
   - Загрузка фотографий (размер, путь, количество)
   - Отправка текста/голосовых сообщений
   - Все переходы состояний FSM

2. **Обработка фотографий:**
   - Скачивание фото из Telegram
   - Сохранение состояния в Redis
   - Конвертация Path → string для Redis

3. **ASR транскрибация:**
   - Скачивание голосового сообщения
   - Размер и формат файла
   - Запрос к ASR API
   - Полный ответ от ASR сервиса
   - Результат транскрибации

4. **LLM умная нормализация:**
   - Запрос к VLLM сервису
   - Анализ типа товара (одежда, электроника, косметика и т.д.)
   - Извлечение характеристик в зависимости от типа товара
   - Приоритизация характеристик и преимуществ
   - normalized_brief: описание задачи на русском языке для пользователя
   - prompt_for_model: детальный промпт на английском языке для Gemini с извлеченными характеристиками
   - Тип изображения и стиль
   - Структура инфографики с учетом best practices

5. **Gemini генерация:**
   - Чтение фотографий
   - Промпт для Gemini
   - Количество сгенерированных изображений

6. **Ошибки:**
   - Полный traceback всех ошибок
   - Тип ошибки и сообщение
   - Контекст (user_id, state, данные)
   - Критические ошибки отправляются владельцу бота (@WuWeiBuild)

### Примеры логов

```
[USER 123456] [COMMAND: /gen] Starting new generation process
[USER 123456] [STATE: WAITING_PHOTOS] Received message type: photo
[USER 123456] Photo downloaded successfully: /tmp/photo_abc123.jpg
[USER 123456] State saved successfully
[USER 123456] [STATE: WAITING_BRIEF] Received message type: voice
[USER 123456] ASR: Starting transcription for file: /tmp/voice_xyz789.ogg
[USER 123456] ASR: Transcription successful: 45 characters
[USER 123456] ASR transcript: Нужно главное фото для Wildberries...
[USER 123456] [STATE: PROCESSING] Starting generation process
[USER 123456] Step 1/3: Normalizing brief with LLM (VLLM)...
[USER 123456] LLM normalization complete. Image type: main_photo, Style: white_background
[USER 123456] Step 2/3: Reading photos as bytes...
[USER 123456] Step 3/3: Generating image with Gemini (NanoBanana)...
[USER 123456] Gemini generated 1 image(s)
[USER 123456] [STATE: SHOW_RESULT] Successfully completed generation
```

### Языковая поддержка

- **Пользовательский интерфейс**: Русский язык
- **ASR транскрибация**: Русский язык
- **LLM умная нормализация**: 
  - Вход: русский язык (текст пользователя)
  - Анализ: определение типа товара и извлечение характеристик
  - Выход: `normalized_brief` на русском языке (для пользователя)
  - Выход: `prompt_for_model` на английском языке (для Gemini API) с извлеченными характеристиками
- **Gemini API**: Английский язык (промпты на английском)
- **Текст в изображениях**: Русский язык (инфографика, надписи, характеристики)

## Диагностика проблем

### Проблема: TelegramConflictError

**Симптом:** `Conflict: terminated by other getUpdates request`

**Решение:** Остановить все запущенные экземпляры бота:
```bash
pkill -f "src.bot.main"
```

### Проблема: Ошибка при загрузке фото

**Что проверить в логах:**
- `[USER {id}] Starting photo download...`
- `[USER {id}] Photo downloaded successfully`
- `[USER {id}] State saved successfully`

Если ошибка — проверьте права доступа к временной директории и доступность Redis.

### Проблема: Ошибка при транскрибации голосового

**Что проверить в логах:**
- `ASR: File extension: {ext}` — формат должен быть в списке поддерживаемых
- `ASR: Response status: {code}` — должен быть 200
- `ASR: Full response: {json}` — проверьте структуру ответа

**Возможные причины:**
- Неподдерживаемый формат аудио
- ASR сервис недоступен (проверьте `http://100.93.82.48:8001/health`)
- Таймаут запроса (увеличьте `ASR_TIMEOUT` в .env)

### Проблема: Ошибка при генерации изображения

**Что проверить в логах:**
- `Step 1/3: Normalizing brief with LLM` — проверьте доступность VLLM
- `Step 2/3: Reading photos as bytes` — проверьте существование файлов
- `Step 3/3: Generating image with Gemini` — проверьте API ключ Gemini

**Возможные причины:**
- VLLM сервис недоступен
- Файлы фото удалены до использования
- Неверный API ключ Gemini
- Таймаут Gemini (увеличьте `GEMINI_TIMEOUT`)

### Просмотр логов в реальном времени

Если бот запущен в терминале, логи выводятся в консоль. Для сохранения в файл:

```bash
cd /Users/aleksandrmordvinov/development/Scanovich-Content && source .venv/bin/activate && PYTHONPATH=. python -m src.bot.main 2>&1 | tee bot.log
```

## Безопасность

- Все секреты хранятся в переменных окружения
- `.env` файл не должен попадать в репозиторий (добавлен в .gitignore)
- API ключи не логируются (только первые символы при необходимости)
- Временные файлы автоматически удаляются после использования

## Лицензия

Приватный проект. Все права защищены.

## Контакты

**Владелец:** @WuWeiBuild  
**Telegram:** [@WuWeiBuild](https://t.me/WuWeiBuild)  
**Бот:** [@scanovich_imagecontent_bot](https://t.me/scanovich_imagecontent_bot)  
**GitHub:** [scanovich-infographic-bot](https://github.com/FUYOH666/scanovich-infographic-bot) (приватный репозиторий)

## История изменений

### v0.1.0 (2025-01-23)
- ✅ Умная система извлечения характеристик в зависимости от типа товара
- ✅ Best practices для инфографики маркетплейсов
- ✅ Сохранение оригинального товара без изменений
- ✅ Безлимитное использование для разработчика
- ✅ Система аналитики и лимитов (10 запросов для обычных пользователей)
- ✅ Команды `/stats` и `/user` для владельца
- ✅ Docker контейнеризация для продакшена (Dockerfile + docker-compose.yml)

